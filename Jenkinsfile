pipeline{
     agent any
        stages{
            stage('Java Version') {
                steps {
                    sh 'java --version'
                    sh './gradlew --version'
                }
            }
       }
    }
